[project]
name = "specification-deserialize"
authors = ["Your Name"]
description = "A Clarity smart contract for structured specification deserialization"
telemetry = false
requirements = []
[contracts.specification-deserializer]
path = "contracts/specification-deserializer.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
